# macOS
.DS_Store

# Go
coverage.out
coverage.html
bin/

# Rust
rust/target/

# wasm-pack output (built by `make wasm`, published as a release asset)
pkg/

# Release packaging output (built by make vectors-package / release.yml)
dist/

# Claude Code worktrees — machine-local execution checkouts, never history.
# Deliberately narrow: .claude/settings.json, agents and skills stay committable.
.claude/worktrees/
